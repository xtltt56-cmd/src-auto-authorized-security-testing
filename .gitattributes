# Full local backup content is stored through Git LFS so GitHub does not reject
# the Docker installer, scanner distributions, or generated validation evidence.
vendor/** filter=lfs diff=lfs merge=lfs -text
validation/** filter=lfs diff=lfs merge=lfs -text
