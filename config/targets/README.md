# Scope files

- `scope_candidate.yaml` is a non-authorizing draft.
- `scope_confirmed.yaml` is acceptable for the owned loopback fixture only.
- For a real SRC, create a separate confirmed file from the platform's current rules; never edit the local-lab file to point at a real host.
